import { StyledHeader, StyledLogo, StyledTitle } from "./styled";

export const Header = () => (
    <StyledHeader>
        <StyledLogo />
        <StyledTitle>Diet composer</StyledTitle>
    </StyledHeader>
);